#pragma once

#include <iostream>
#include <vector>
#include <string>
#include <unordered_map>

namespace twosum {
    std::vector<int> twoSum(std::vector<int>& nums, int target);
} // namespace twosum